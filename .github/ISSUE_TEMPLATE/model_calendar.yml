name: "Add/Modify: Model Calendar"
description: Add or modify a model calendar in WCRP Universe
title: "Add/Modify: Model Calendar"
labels: ['delta', 'model-calendar', 'Review', 'keep-open']
body:
  - type: markdown
    attributes:
      value: |
        # Add/Modify: Model Calendar

  - type: input
    id: validation_key
    attributes:
      label: Validation Key
      description: Unique identifier
      placeholder: "e.g. gregorian"
    validations:
      required: true

  - type: input
    id: ui_label
    attributes:
      label: UI Label
      description: Full name
      placeholder: "e.g. Gregorian"

  - type: textarea
    id: description
    attributes:
      label: Description
      description: Detailed description

  - type: dropdown
    id: issue_kind
    attributes:
      label: Issue Kind
      description: New or modify existing
      options:
        - "New"
        - "Modify"
      default: 0
    validations:
      required: true

  - type: input
    id: collaborators
    attributes:
      label: Additional Collaborators
      description: |
        To add attribution to additional members include their GitHub usernames (only) separated by a comma.
      placeholder: "e.g. user1"
